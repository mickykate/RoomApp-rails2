<h2>プロフィール編集の画面</h2>

<%= form_for (resource, as: resource_name, url: session_path(resource_name))  do |f| %>

<div class="field">
    <%= f.label :icon %>
    <%= f.file_field :icon %>
</div>

<div class="field">
    <%= f.label :username %><br />
    <%= f.text_field :username %>
</div>

#自己紹介のカラム(itroduction)追加 1217
<div class="field">
    <%= f.label :introduction %><br />
    <%= f.text_area :introduction %>
</div>

<%= render "devise/shared/footer" %>
